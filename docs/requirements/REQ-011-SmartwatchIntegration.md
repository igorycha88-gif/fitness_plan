# REQ-011: Интеграция со смарт-часами для получения данных тренировки

**Status**: New
**Priority**: High
**Complexity**: Complex
**Created**: 2026-02-17
**Updated**: 2026-02-17

---

## Description

Реализация интеграции приложения со смарт-часами для получения биометрических данных в реальном времени во время выполнения тренировки. Интеграция позволит пользователям видеть свои показатели (пульс, калории и др.) непосредственно в приложении и сохранять их в историю тренировок для последующего анализа.

---

## User Story

Как пользователь приложения Fitness Plan,
Я хочу получать данные с моих смарт-часов во время тренировки (пульс, калории),
Чтобы видеть свои биометрические показатели в реальном времени, анализировать интенсивность тренировки и отслеживать прогресс.

---

## Current State Analysis

### Существующая реализация

На данный момент приложение:
- Отслеживает выполнение упражнений (isCompleted)
- Сохраняет статистику весов и подходов
- Не имеет интеграции с носимыми устройствами
- Не получает биометрические данные

### Затронутые компоненты

| Компонент | Файл | Изменения |
|-----------|------|-----------|
| ExerciseDetailScreen | `ui/ExerciseDetailScreen.kt` | Отображение данных с часов |
| ExerciseStats | `domain/model/ExerciseStats.kt` | Добавление полей для биометрии |
| ExerciseStatsRepository | `data/ExerciseStatsRepository.kt` | Сохранение биометрических данных |
| Новый: HealthConnectRepository | `data/HealthConnectRepository.kt` | Интеграция с Health Connect |
| Новый: SmartwatchDataModel | `domain/model/SmartwatchData.kt` | Модель данных с часов |

---

## Market Analysis: Поддерживаемые платформы

### Android Health Connect API (Рекомендуется)

**Преимущества:**
- Универсальный API для Android 14+ (API 34+)
- Поддержка Samsung Health, Google Fit, Fitbit, Garmin и других
- Встроен в Android, не требует сторонних SDK
- Единый интерфейс для разных производителей часов

**Поддерживаемые данные:**
| Тип данных | Health Connect Type | Доступность |
|------------|---------------------|-------------|
| Пульс | HeartRateRecord | Чтение |
| Калории | ActiveCaloriesBurnedRecord | Чтение |
| Шаги | StepsRecord | Чтение |
| Расстояние | DistanceRecord | Чтение |
| Зоны пульса | HeartRateRecord (расчет) | Вычисление |
| Длительность | ExerciseSessionRecord | Чтение |

**Ограничения:**
- Требует Android 14+ (API 34), для более старых версий - Google Fit
- Требует разрешений пользователя
- Не все производители поддерживают все типы данных

### Альтернативы (не рекомендуется для MVP)

1. **Samsung Health SDK** - только для Samsung устройств
2. **Google Fit API** - устаревший, Google рекомендует Health Connect
3. **Wear OS Companion App** - требует разработки отдельного приложения для часов

---

## UI/UX: Расположение и формат отображения

### Обзор мест отображения

| Экран | Тип данных | Формат | Обновление |
|-------|------------|--------|------------|
| ExerciseDetailScreen | Реальное время | Компактная карточка | 1 сек |
| HomeScreen (карточка дня) | Суммарно за тренировку | Inline метрики | При завершении упражнения |
| StatisticsScreen | История | Графики + таблицы | При просмотре статистики |

---

### REQ-011.UI.1: ExerciseDetailScreen - Карточка реального времени

**Расположение**: В верхней части экрана, под заголовком упражнения

**Макет (подключено)**:
```
┌─────────────────────────────────────────────────────┐
│  [<-]  Жим лёжа                        [Инструкция] │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │ 📱 Часы подключены                          │    │
│  ├─────────────────────────────────────────────┤    │
│  │  ❤️ 128 уд/мин    │    🔥 156 ккал          │    │
│  │  ● Кардио зона   │    ⏱️ 12:34             │    │
│  └─────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────┤
│  Подходы и повторения                               │
│  ...                                                │
└─────────────────────────────────────────────────────┘
```

**Макет (не подключено)**:
```
┌─────────────────────────────────────────────────────┐
│  [<-]  Жим лёжа                        [Инструкция] │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │ 📱 Смарт-часы не подключены                 │    │
│  │ Подключите для отслеживания пульса и калорий │    │
│  │                              [Подключить]   │    │
│  └─────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────┤
│  Подходы и повторения                               │
│  ...                                                │
└─────────────────────────────────────────────────────┘
```

**Компонент Composable**:
```kotlin
@Composable
fun SmartwatchDataCard(
    smartwatchData: SmartwatchData?,
    isConnected: Boolean,
    isAvailable: Boolean,
    onConnectClick: () -> Unit,
    modifier: Modifier = Modifier
) {
    Card(
        modifier = modifier.fillMaxWidth(),
        colors = CardDefaults.cardColors(
            containerColor = when {
                !isAvailable -> MaterialTheme.colorScheme.surfaceVariant
                isConnected -> MaterialTheme.colorScheme.primaryContainer
                else -> MaterialTheme.colorScheme.surfaceVariant
            }
        )
    ) {
        when {
            !isAvailable -> NotAvailableContent()
            !isConnected -> DisconnectedContent(onConnectClick)
            smartwatchData != null -> ConnectedContent(smartwatchData)
        }
    }
}
```

**Цвета зон пульса**:
| Зона | Цвет фона | Цвет текста | Badge |
|------|-----------|-------------|-------|
| REST | Grey 100 | Grey 700 | ⚪ |
| FAT_BURN | Green 100 | Green 700 | 🟢 |
| CARDIO | Yellow 100 | Yellow 900 | 🟡 |
| HARD | Orange 100 | Orange 900 | 🟠 |
| MAXIMUM | Red 100 | Red 700 | 🔴 |
| EXTREME | Purple 100 | Purple 700 | 🟣 |

---

### REQ-011.UI.2: HomeScreen - Карточка дня тренировки

**Расположение**: В свернутом и развернутом виде карточки дня

**Макет (свернутый вид - суммарные данные за день)**:
```
┌─────────────────────────────────────────────────────┐
│  День 1 - Грудь, Трицепс                           │
│  12 упражнений │ ❤️ 456 ккал │ 🔥 156 уд/мин ср    │
│  [✓] 8/12 выполнено                                │
└─────────────────────────────────────────────────────┘
```

**Макет (развернутый вид - данные по упражнениям)**:
```
┌─────────────────────────────────────────────────────┐
│  ▼ День 1 - Грудь, Трицепс                [📅]     │
├─────────────────────────────────────────────────────┤
│  ═══ Суммарно за тренировку ═══                    │
│  ❤️ Средний пульс: 134 уд/мин  🔥 Калории: 456 ккал│
│  📊 Зоны: Жиросжигание (45%), Кардио (35%), ...    │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │ [✓] Жим лёжа          4×10   60 кг          │    │
│  │     ❤️ 128 уд/мин  │  🔥 45 ккал            │    │
│  └─────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────┐    │
│  │ [ ] Отжимания          3×15   —             │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

**Inline компонент для упражнения**:
```kotlin
@Composable
fun ExerciseBiometricsInline(
    avgHeartRate: Int?,
    calories: Double?,
    modifier: Modifier = Modifier
) {
    if (avgHeartRate != null || calories != null) {
        Row(
            modifier = modifier,
            horizontalArrangement = Arrangement.spacedBy(12.dp)
        ) {
            avgHeartRate?.let {
                Row(verticalAlignment = Alignment.CenterVertically) {
                    Text("❤️", fontSize = 12.sp)
                    Text(" $it уд/мин", style = MaterialTheme.typography.labelSmall)
                }
            }
            calories?.let {
                Row(verticalAlignment = Alignment.CenterVertically) {
                    Text("🔥", fontSize = 12.sp)
                    Text(" ${it.toInt()} ккал", style = MaterialTheme.typography.labelSmall)
                }
            }
        }
    }
}
```

---

### REQ-011.UI.3: StatisticsScreen - Исторические данные

**Расположение**: Новая вкладка или секция в StatisticsScreen

**Макет (графики)**:
```
┌─────────────────────────────────────────────────────┐
│  📊 Статистика тренировок                          │
├─────────────────────────────────────────────────────┤
│  [Веса] [Объем] [Частота] [Биометрия] ← Новая     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Средний пульс по тренировкам                      │
│  ┌─────────────────────────────────────────────┐    │
│  │     📈                                       │    │
│  │   150 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─            │    │
│  │   130 ─ ─ ─ ─╲╱─╲─╱─╲╱─ ─ ─ ─             │    │
│  │   110 ─ ─╱─╱──────╲───╲─ ─ ─              │    │
│  │    90 ─╱─────────────────╲─ ─              │    │
│  │     Пн  Ср  Пт  Пн  Ср  Пт                 │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  Калории за неделю: 2,340 ккал (+15% ↑)           │
│  Средняя зона: Кардио                              │
│                                                     │
│  ═══ Распределение по зонам ═══                   │
│  ┌─────────────────────────────────────────────┐    │
│  │ Жиросжигание ████████████░░░ 45%            │    │
│  │ Кардио      ████████░░░░░░░ 35%             │    │
│  │ Интенсивная ████░░░░░░░░░░░ 15%             │    │
│  │ Максимальная ██░░░░░░░░░░░░ 5%              │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  ═══ По упражнениям (топ-5 по калориям) ═══       │
│  ┌─────────────────────────────────────────────┐    │
│  │ 1. Приседания          520 ккал │ 142 ср HR │    │
│  │ 2. Становая тяга       480 ккал │ 138 ср HR │    │
│  │ 3. Жим лёжа            320 ккал │ 125 ср HR │    │
│  │ 4. Тяга верхнего блока 280 ккал │ 118 ср HR │    │
│  │ 5. Отжимания           240 ккал │ 122 ср HR │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

**Новый компонент графика**:
```kotlin
@Composable
fun HeartRateChart(
    data: List<HeartRateDataPoint>,
    modifier: Modifier = Modifier
)

@Composable
fun CaloriesChart(
    data: List<CaloriesDataPoint>,
    modifier: Modifier = Modifier
)

@Composable
fun HeartRateZoneDistribution(
    zones: Map<HeartRateZone, Int>,  // zone -> percentage
    modifier: Modifier = Modifier
)

@Composable
fun TopExercisesByCalories(
    exercises: List<ExerciseCaloriesStats>,
    modifier: Modifier = Modifier
)
```

---

### REQ-011.UI.4: Сравнение с предыдущей тренировкой

**Расположение**: ExerciseDetailScreen, под карточкой реального времени

**Макет**:
```
┌─────────────────────────────────────────────────────┐
│  📱 Часы подключены  (текущая тренировка)          │
│  ❤️ 128 уд/мин  │  🔥 156 ккал  │  ⏱️ 12:34       │
├─────────────────────────────────────────────────────┤
│  📈 По сравнению с прошлый раз:                    │
│  • Пульс: 128 vs 135 (↓7 уд/мин) ✓ Легче         │
│  • Калории: 156 vs 142 (↑14 ккал) ↑ Эффективнее   │
└─────────────────────────────────────────────────────┘
```

**Компонент**:
```kotlin
@Composable
fun BiometricsComparison(
    current: SmartwatchData?,
    previous: ExerciseStats?,
    modifier: Modifier = Modifier
)
```

---

## Technical Deep Dive: Как работает подключение к часам

### Архитектура: Health Connect как посредник

**Важно понимать**: Приложение НЕ подключается к часам напрямую.

```
┌──────────────┐   Bluetooth/WiFi   ┌─────────────────────┐
│ Смарт-часы   │ ──────────────────▶│ Приложение-провайдер│
│ Samsung      │                    │ Samsung Health      │
│ Garmin       │                    │ Google Fit          │
│ Fitbit       │                    │ Garmin Connect      │
│ Xiaomi       │                    │ Fitbit App          │
└──────────────┘                    └──────────┬──────────┘
                                               │
                                               │ Автоматическая
                                               │ синхронизация
                                               ▼
                                    ┌─────────────────────┐
                                    │   Health Connect    │
                                    │   (системный API    │
                                    │    Android 14+)     │
                                    │                     │
                                    │ Хранилище здоровь-  │
                                    │ яных данных         │
                                    └──────────┬──────────┘
                                               │
                                               │ Запрос на чтение
                                               ▼
                                    ┌─────────────────────┐
                                    │   Fitness Plan      │
                                    │   (наше приложение) │
                                    └─────────────────────┘
```

### Последовательность проверки и подключения

```
┌─────────────────────────────────────────────────────────────────┐
│                    ПРОВЕРКА И ПОДКЛЮЧЕНИЕ                       │
└─────────────────────────────────────────────────────────────────┘

ШАГ 1: Проверка доступности Health Connect
────────────────────────────────────────────
┌─────────────────┐
│ Fitness Plan    │ ──▶ HealthConnectClient.getOrCreate(context)
└─────────────────┘
         │
         ▼
    ┌────────────────────────────────────┐
    │ Health Connect установлен?         │
    │ (есть ли на устройстве провайдер)  │
    └────────────────────────────────────┘
         │
    ┌────┴────┐
    ▼         ▼
   YES        NO → Показать: "Установите Samsung Health или Google Fit"


ШАГ 2: Проверка разрешений
──────────────────────────
┌─────────────────┐
│ Fitness Plan    │ ──▶ healthConnectClient.permissionController
└─────────────────┘         .getGrantedPermissions()
         │
         ▼
    ┌────────────────────────────────────┐
    │ Разрешения уже предоставлены?      │
    │ (READ_HEART_RATE, READ_ACTIVE_     │
    │  CALORIES_BURNED, ...)             │
    └────────────────────────────────────┘
         │
    ┌────┴────┐
    ▼         ▼
   YES        NO → Запрос разрешений через Intent


ШАГ 3: Запрос разрешений (если не предоставлены)
────────────────────────────────────────────────
┌─────────────────┐
│ Fitness Plan    │ ──▶ val requestPermissionContract =
└─────────────────┘     PermissionController.createRequestPermissionActivityContract()
         │
         ▼
    ┌────────────────────────────────────┐
    │ Открывается системный диалог:      │
    │                                    │
    │ "Fitness Plan хочет читать:        │
    │  • Пульс                           │
    │  • Калории                         │
    │  • Шаги"                           │
    │                                    │
    │  [Разрешить]  [Запретить]          │
    └────────────────────────────────────┘


ШАГ 4: Проверка наличия данных (часы синхронизированы?)
────────────────────────────────────────────────────────
┌─────────────────┐
│ Fitness Plan    │ ──▶ Запрос последних данных
└─────────────────┘
         │
         ▼
    healthConnectClient.readRecords(
        ReadRecordsRequest(
            recordType = HeartRateRecord::class,
            timeRangeFilter = TimeRangeFilter.after(
                Instant.now().minusSeconds(60)
            )
        )
    )
         │
         ▼
    ┌────────────────────────────────────┐
    │ Есть данные за последние 60 сек?   │
    └────────────────────────────────────┘
         │
    ┌────┴────┐
    ▼         ▼
   YES        NO → "Часы не подключены или не синхронизированы"
   │
   ▼
   Показываем данные в UI
```

### Код: Проверка доступности и разрешений

```kotlin
class HealthConnectRepositoryImpl @Inject constructor(
    @ApplicationContext private val context: Context
) : HealthConnectRepository {

    private val healthConnectClient: HealthConnectClient by lazy {
        HealthConnectClient.getOrCreate(context)
    }

    // ШАГ 1: Проверка доступности Health Connect
    override suspend fun checkAvailability(): Boolean {
        return try {
            val availabilityStatus = HealthConnectClient
                .getSdkStatus(context, "com.example.fitness_plan")
            
            when (availabilityStatus) {
                HealthConnectClient.SDK_UNAVAILABLE -> false
                HealthConnectClient.SDK_UNAVAILABLE_PROVIDER_UPDATE_REQUIRED -> false
                else -> true  // SDK_AVAILABLE
            }
        } catch (e: Exception) {
            false
        }
    }

    // ШАГ 2: Проверка разрешений
    override suspend fun hasPermissions(): Boolean {
        return try {
            val granted = healthConnectClient.permissionController
                .getGrantedPermissions()
            
            REQUIRED_PERMISSIONS.all { it in granted }
        } catch (e: Exception) {
            false
        }
    }

    // ШАГ 3: Запрос разрешений (вызывается из Activity)
    // Используется PermissionController.createRequestPermissionActivityContract()

    // ШАГ 4: Проверка наличия недавних данных (косвенная проверка часов)
    override suspend fun hasRecentData(): Boolean {
        return try {
            val now = Instant.now()
            val oneMinuteAgo = now.minusSeconds(60)

            val request = ReadRecordsRequest(
                recordType = HeartRateRecord::class,
                timeRangeFilter = TimeRangeFilter.between(oneMinuteAgo, now),
                pageSize = 1
            )
            
            val response = healthConnectClient.readRecords(request)
            response.records.isNotEmpty()
        } catch (e: Exception) {
            false
        }
    }

    companion object {
        val REQUIRED_PERMISSIONS = setOf(
            HealthPermission.getReadPermission(HeartRateRecord::class),
            HealthPermission.getReadPermission(ActiveCaloriesBurnedRecord::class),
            HealthPermission.getReadPermission(StepsRecord::class),
            HealthPermission.getReadPermission(DistanceRecord::class)
        )
    }
}
```

### Код: Получение данных в реальном времени

```kotlin
class HealthConnectRepositoryImpl @Inject constructor(
    @ApplicationContext private val context: Context
) : HealthConnectRepository {

    // Получение текущих данных (периодический опрос)
    override suspend fun getCurrentData(): SmartwatchData? {
        return try {
            val now = Instant.now()
            val sessionStart = now.minusSeconds(activeSessionDuration) // Время начала сессии

            // Получаем пульс
            val heartRateRecords = healthConnectClient.readRecords(
                ReadRecordsRequest(
                    recordType = HeartRateRecord::class,
                    timeRangeFilter = TimeRangeFilter.between(sessionStart, now)
                )
            )

            // Получаем калории
            val caloriesRecords = healthConnectClient.readRecords(
                ReadRecordsRequest(
                    recordType = ActiveCaloriesBurnedRecord::class,
                    timeRangeFilter = TimeRangeFilter.between(sessionStart, now)
                )
            )

            // Агрегируем данные
            val heartRates = heartRateRecords.records
                .flatMap { it.samples }
                .map { it.beatsPerMinute.toInt() }
            
            val totalCalories = caloriesRecords.records
                .sumOf { it.energy.inKilocalories }

            SmartwatchData(
                timestamp = now.toEpochMilli(),
                heartRate = heartRates.lastOrNull(),
                heartRateMin = heartRates.minOrNull(),
                heartRateMax = heartRates.maxOrNull(),
                heartRateAvg = heartRates.average().toInt(),
                caloriesBurned = totalCalories,
                activeDuration = activeSessionDuration * 1000,
                heartRateZone = calculateHeartRateZone(heartRates.lastOrNull())
            )
        } catch (e: Exception) {
            null
        }
    }

    // Реактивный поток данных (каждую секунду)
    override fun getRealtimeDataFlow(): Flow<SmartwatchData> = flow {
        while (true) {
            val data = getCurrentData()
            if (data != null) {
                emit(data)
            }
            delay(1000) // Опрос каждую секунду
        }
    }.flowOn(Dispatchers.IO)

    private fun calculateHeartRateZone(hr: Int?): HeartRateZone? {
        if (hr == null || userAge == null) return null
        
        val maxHr = 220 - userAge
        val percentage = hr.toDouble() / maxHr
        
        return when {
            percentage < 0.5 -> HeartRateZone.REST
            percentage < 0.6 -> HeartRateZone.FAT_BURN
            percentage < 0.7 -> HeartRateZone.CARDIO
            percentage < 0.8 -> HeartRateZone.HARD
            percentage < 0.9 -> HeartRateZone.MAXIMUM
            else -> HeartRateZone.EXTREME
        }
    }
}
```

### ViewModel: Управление сессией мониторинга

```kotlin
@HiltViewModel
class ExerciseDetailViewModel @Inject constructor(
    private val healthConnectRepository: HealthConnectRepository
) : ViewModel() {

    private val _healthConnectState = MutableStateFlow<HealthConnectState>(HealthConnectState.Checking)
    val healthConnectState: StateFlow<HealthConnectState> = _healthConnectState

    private val _smartwatchData = MutableStateFlow<SmartwatchData?>(null)
    val smartwatchData: StateFlow<SmartwatchData?> = _smartwatchData

    private var monitoringJob: Job? = null

    sealed class HealthConnectState {
        object Checking : HealthConnectState()
        object NotAvailable : HealthConnectState()
        data class Available(val hasPermissions: Boolean) : HealthConnectState()
        data class Connected(val lastDataTime: Long) : HealthConnectState()
        data class Error(val message: String) : HealthConnectState()
    }

    init {
        checkHealthConnect()
    }

    fun checkHealthConnect() {
        viewModelScope.launch {
            _healthConnectState.value = HealthConnectState.Checking

            val isAvailable = healthConnectRepository.checkAvailability()
            if (!isAvailable) {
                _healthConnectState.value = HealthConnectState.NotAvailable
                return@launch
            }

            val hasPermissions = healthConnectRepository.hasPermissions()
            _healthConnectState.value = HealthConnectState.Available(hasPermissions)

            if (hasPermissions) {
                val hasRecentData = healthConnectRepository.hasRecentData()
                if (hasRecentData) {
                    startMonitoring()
                }
            }
        }
    }

    fun startMonitoring() {
        monitoringJob?.cancel()
        monitoringJob = viewModelScope.launch {
            healthConnectRepository.getRealtimeDataFlow()
                .catch { e ->
                    _healthConnectState.value = HealthConnectState.Error(e.message ?: "Unknown error")
                }
                .collect { data ->
                    _smartwatchData.value = data
                    _healthConnectState.value = HealthConnectState.Connected(data.timestamp)
                }
        }
    }

    fun stopMonitoring(): SmartwatchData? {
        monitoringJob?.cancel()
        return _smartwatchData.value
    }
}
```

### UI: Запрос разрешений

```kotlin
// В Activity или Fragment
private val requestPermissionLauncher = registerForActivityResult(
    PermissionController.createRequestPermissionActivityContract()
) { grantedPermissions ->
    if (grantedPermissions.containsAll(HealthConnectRepositoryImpl.REQUIRED_PERMISSIONS)) {
        viewModel.startMonitoring()
    }
}

fun requestHealthPermissions() {
    requestPermissionLauncher.launch(HealthConnectRepositoryImpl.REQUIRED_PERMISSIONS)
}
```

### Важные ограничения Health Connect

| Ограничение | Описание | Решение |
|-------------|----------|---------|
| Нет real-time push | Health Connect не отправляет уведомления о новых данных | Периодический опрос (polling) |
| Задержка данных | Данные могут поступать с задержкой 1-30 сек | Показывать время последнего обновления |
| Нет прямой связи с часами | Работает только через провайдеров (Samsung Health, Google Fit) | Проверять наличие провайдера |
| Пользователь может отозвать разрешения | В любой момент через настройки | Проверять перед каждой сессией |

### Как проверить, что часы действительно подключены

Прямого способа проверить подключение часов нет. Косвенные признаки:

1. **Есть данные за последние 60 секунд** → часы работают
2. **Нет данных более 5 минут** → возможно, часы отключены
3. **Рекомендация**: Показывать время последнего полученного измерения

```kotlin
// UI состояние
data class SmartwatchUiState(
    val isConnected: Boolean,
    val lastUpdateTime: Long,  // timestamp последнего измерения
    val data: SmartwatchData?
) {
    val statusText: String
        get() = when {
            !isConnected -> "Часы не подключены"
            System.currentTimeMillis() - lastUpdateTime > 300_000 -> "Нет данных более 5 мин"
            System.currentTimeMillis() - lastUpdateTime > 60_000 -> "Последнее измерение: ${formatTime(lastUpdateTime)}"
            else -> "Часы подключены"
        }
}
```

---

## Requirements

### REQ-011.1: Базовая архитектура интеграции

**Приоритет**: Critical

**Описание**:
Создать архитектурную основу для интеграции с Health Connect API, включая новый репозиторий, модели данных и DI конфигурацию.

**Технические требования**:

1. Добавить зависимость Health Connect в `build.gradle.kts`:
```kotlin
implementation("androidx.health.connect:connect-client:1.1.0-alpha07")
```

2. Создать модель данных `SmartwatchData`:
```kotlin
data class SmartwatchData(
    val timestamp: Long,
    val heartRate: Int?,              // ударов в минуту
    val heartRateMin: Int?,           // минимальный пульс за сессию
    val heartRateMax: Int?,           // максимальный пульс за сессию
    val heartRateAvg: Int?,           // средний пульс
    val caloriesBurned: Double?,      // ккал
    val steps: Int?,                  // шаги (для кардио)
    val distance: Double?,            // метры
    val activeDuration: Long,         // миллисекунды активной фазы
    val heartRateZone: HeartRateZone? // текущая зона пульса
)

enum class HeartRateZone {
    REST,           // < 50% max HR - отдых
    FAT_BURN,       // 50-60% max HR - жиросжигание
    CARDIO,         // 60-70% max HR - кардио
    HARD,           // 70-80% max HR - интенсивная
    MAXIMUM,        // 80-90% max HR - максимальная
    EXTREME         // > 90% max HR - экстремальная
}
```

3. Создать интерфейс репозитория `HealthConnectRepository`:
```kotlin
interface HealthConnectRepository {
    suspend fun checkAvailability(): Boolean
    suspend fun requestPermissions(): Boolean
    suspend fun hasPermissions(): Boolean
    suspend fun startSession()
    suspend fun stopSession(): SmartwatchSessionSummary?
    suspend fun getCurrentData(): SmartwatchData?
    fun getRealtimeDataFlow(): Flow<SmartwatchData>
}
```

4. Создать реализацию `HealthConnectRepositoryImpl`:
```kotlin
class HealthConnectRepositoryImpl @Inject constructor(
    @ApplicationContext private val context: Context
) : HealthConnectRepository {
    private val healthConnectClient: HealthConnectClient by lazy {
        HealthConnectClient.getOrCreate(context)
    }
    // ...
}
```

5. Добавить в AndroidManifest.xml:
```xml
<uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
<uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
<uses-permission android:name="android.permission.health.READ_STEPS"/>
<uses-permission android:name="android.permission.health.READ_DISTANCE"/>

<activity android:name=".HealthConnectPermissionActivity">
    <intent-filter>
        <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
    </intent-filter>
</activity>
```

6. Обновить `AppModule.kt` для DI:
```kotlin
@Module
@InstallIn(SingletonComponent::class)
abstract class RepositoryModule {
    @Binds
    @Singleton
    abstract fun bindHealthConnectRepository(
        impl: HealthConnectRepositoryImpl
    ): HealthConnectRepository
}
```

**Расположение**: 
- `domain/model/SmartwatchData.kt` (новый)
- `domain/repository/HealthConnectRepository.kt` (новый)
- `data/HealthConnectRepositoryImpl.kt` (новый)

---

### REQ-011.2: Расширение модели ExerciseStats

**Приоритет**: High

**Описание**:
Добавить поля для хранения биометрических данных в модель ExerciseStats для сохранения истории.

**Текущее состояние** (пример модели):
```kotlin
data class ExerciseStats(
    val exerciseId: String,
    val lastWeight: Float?,
    val lastReps: String?,
    val totalCompletions: Int,
    val lastCompletedDate: Long?
    // ...
)
```

**Ожидаемое состояние**:
```kotlin
data class ExerciseStats(
    val exerciseId: String,
    val lastWeight: Float?,
    val lastReps: String?,
    val totalCompletions: Int,
    val lastCompletedDate: Long?,
    // Новые поля для биометрии
    val lastHeartRateAvg: Int? = null,
    val lastHeartRateMax: Int? = null,
    val lastHeartRateMin: Int? = null,
    val lastCaloriesBurned: Double? = null,
    val lastActiveDuration: Long? = null,
    val lastHeartRateZone: String? = null
)
```

**Расположение**: `domain/model/ExerciseStats.kt`

---

### REQ-011.3: Экран деталей упражнения - отображение данных с часов

**Приоритет**: High

**Описание**:
Добавить UI компонент для отображения биометрических данных в реальном времени на экране выполнения упражнения.

**Требования к UI**:

1. Компонент `SmartwatchDataCard`:
```kotlin
@Composable
fun SmartwatchDataCard(
    smartwatchData: SmartwatchData?,
    isAvailable: Boolean,
    onRequestConnect: () -> Unit,
    modifier: Modifier = Modifier
)
```

2. Отображаемые данные (при наличии подключения):
   - Текущий пульс с цветовой индикацией зоны (бейдж цвета зоны)
   - Калории за сессию
   - Длительность активной фазы
   - Индикатор подключения к часам

3. При отсутствии подключения:
   - Сообщение "Подключите смарт-часы для отслеживания показателей"
   - Кнопка "Подключить" (открывает настройки Health Connect)

**Визуальный дизайн**:
```
┌────────────────────────────────────────┐
│ 📱 Смарт-часы подключены              │
├────────────────────────────────────────┤
│  ❤️ 128 уд/мин        🔥 156 ккал      │
│  [Кардио зона]        ⏱️ 12:34         │
└────────────────────────────────────────┘
```

**Цветовая схема зон пульса**:
| Зона | Цвет | Hex |
|------|------|-----|
| REST | Серый | #9E9E9E |
| FAT_BURN | Зеленый | #4CAF50 |
| CARDIO | Желтый | #FFC107 |
| HARD | Оранжевый | #FF9800 |
| MAXIMUM | Красный | #F44336 |
| EXTREME | Фиолетовый | #9C27B0 |

**Расположение**: `ui/ExerciseDetailScreen.kt`

---

### REQ-011.4: Управление сессией тренировки

**Приоритет**: High

**Описание**:
Реализовать логику управления сессией получения данных с часов при начале и завершении тренировки.

**Жизненный цикл сессии**:

1. **При открытии ExerciseDetailScreen**:
   - Проверить доступность Health Connect
   - Проверить наличие разрешений
   - Если есть разрешения - начать сессию мониторинга
   - Если нет - показать кнопку запроса разрешений

2. **Во время выполнения упражнения**:
   - Получать данные в реальном времени через Flow
   - Обновлять UI каждые 1-2 секунды
   - Накапливать статистику сессии

3. **При завершении упражнения (isCompleted = true)**:
   - Остановить сессию
   - Получить итоговую статистику
   - Сохранить в ExerciseStats

**Новый ViewModel метод**:
```kotlin
@HiltViewModel
class ExerciseDetailViewModel @Inject constructor(
    private val healthConnectRepository: HealthConnectRepository,
    private val exerciseStatsRepository: ExerciseStatsRepository
) : ViewModel() {
    
    private val _smartwatchData = MutableStateFlow<SmartwatchData?>(null)
    val smartwatchData: StateFlow<SmartwatchData?> = _smartwatchData
    
    private val _healthConnectAvailable = MutableStateFlow(false)
    val healthConnectAvailable: StateFlow<Boolean> = _healthConnectAvailable
    
    fun startHealthMonitoring() { ... }
    fun stopHealthMonitoring() { ... }
    fun requestHealthPermissions() { ... }
}
```

**Расположение**: 
- `presentation/viewmodel/ExerciseDetailViewModel.kt` (новый или расширение)
- `ui/ExerciseDetailScreen.kt`

---

### REQ-011.5: Сохранение биометрических данных в статистику

**Приоритет**: Medium

**Описание**:
Обновить репозиторий ExerciseStatsRepository для сохранения биометрических данных при завершении упражнения.

**Изменения в ExerciseStatsRepository**:
```kotlin
suspend fun saveExerciseCompletionWithBiometrics(
    username: String,
    exerciseId: String,
    weight: Float?,
    reps: String?,
    smartwatchData: SmartwatchData?
)
```

**Обновление данных**:
- Сохранять средние/макс/мин показатели пульса
- Сохранять калории
- Сохранять длительность активной фазы
- Сохранять преобладающую зону пульса

**Расположение**: `data/ExerciseStatsRepository.kt`

---

### REQ-011.6: Отображение исторических данных в статистике

**Приоритет**: Low (Phase 2)

**Описание**:
Добавить отображение биометрических данных в экране статистики упражнения.

**Требования**:
- Показывать средние показатели пульса за последние тренировки
- Показывать тренд калорий
- Добавить график зон пульса

**Расположение**: `ui/StatisticsScreen.kt`, `ui/charts/`

---

### REQ-011.7: Обработка ошибок и edge cases

**Приоритет**: Medium

**Описание**:
Реализовать корректную обработку сценариев ошибок и граничных случаев.

**Сценарии обработки**:

| Сценарий | Поведение |
|----------|-----------|
| Health Connect недоступен | Скрыть компонент данных, показать сообщение |
| Отказ в разрешениях | Показать кнопку "Настройки" для ручного включения |
| Часы отключены | Показать "Часы не подключены", продолжить без данных |
| Потеря соединения | Показать индикатор переподключения |
| Некорректные данные | Игнорировать, не крашить приложение |
| Фоновый режим | Продолжить сбор данных, восстановить при возврате |

---

## Data Flow Diagram

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  Smart Watch    │────▶│  Health Connect  │────▶│  HealthConnect  │
│  (Samsung/Garmin│     │  (Android API)   │     │  Repository     │
│   /Fitbit/etc)  │     │                  │     │                 │
└─────────────────┘     └──────────────────┘     └────────┬────────┘
                                                          │
                                                          ▼
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  ExerciseDetail │◀────│  ExerciseDetail  │◀────│  Flow<Smartwatch│
│     Screen      │     │    ViewModel     │     │     Data>       │
│  (Display UI)   │     │                  │     │                 │
└────────┬────────┘     └──────────────────┘     └─────────────────┘
         │
         │ onComplete
         ▼
┌─────────────────┐     ┌──────────────────┐
│  ExerciseStats  │────▶│    DataStore     │
│   Repository    │     │   (Persistence)  │
└─────────────────┘     └──────────────────┘
```

---

## Acceptance Criteria

### Базовая функциональность
- [ ] **AC-011.1**: Приложение проверяет доступность Health Connect на устройстве
- [ ] **AC-011.2**: Пользователь может предоставить разрешения на чтение биометрических данных

### ExerciseDetailScreen (реальное время)
- [ ] **AC-011.3**: Компактная карточка отображается под заголовком упражнения
- [ ] **AC-011.4**: Пульс отображается в реальном времени с цветовой индикацией зоны
- [ ] **AC-011.5**: Калории отображаются в реальном времени
- [ ] **AC-011.6**: Длительность активной фазы отображается в формате MM:SS
- [ ] **AC-011.7**: При отсутствии подключения показывается кнопка "Подключить"
- [ ] **AC-011.8**: Цвет фона карточки меняется в зависимости от статуса (подключено/не подключено)

### HomeScreen - карточка дня (суммарные данные)
- [ ] **AC-011.9**: В свернутом виде дня показываются суммарные калории и средний пульс
- [ ] **AC-011.10**: В развернутом виде показывается секция "Суммарно за тренировку"
- [ ] **AC-011.11**: Рядом с каждым выполненным упражнением показаны inline метрики (пульс, калории)
- [ ] **AC-011.12**: Метрики отображаются только если есть сохраненные данные

### StatisticsScreen (исторические данные)
- [ ] **AC-011.13**: Добавлена вкладка/секция "Биометрия"
- [ ] **AC-011.14**: График среднего пульса по тренировкам отображается корректно
- [ ] **AC-011.15**: Суммарные калории за период отображаются с трендом
- [ ] **AC-011.16**: Распределение по зонам пульса отображается в виде прогресс-баров
- [ ] **AC-011.17**: Топ-5 упражнений по калориям отображается списком

### Сравнение с предыдущей тренировкой
- [ ] **AC-011.18**: Под карточкой реального времени показывается сравнение
- [ ] **AC-011.19**: Показывается разница пульса (текущий vs прошлый)
- [ ] **AC-011.20**: Показывается разница калорий (текущий vs прошлый)
- [ ] **AC-011.21**: Улучшения отмечаются зеленым, ухудшения - оранжевым

### Сохранение данных
- [ ] **AC-011.22**: При завершении упражнения биометрические данные сохраняются
- [ ] **AC-011.23**: Сохраняются средний, минимальный и максимальный пульс
- [ ] **AC-011.24**: Сохраняется количество сожженных калорий
- [ ] **AC-011.25**: Сохраняется длительность активной фазы
- [ ] **AC-011.26**: Сохраняется преобладающая зона пульса

### Обработка ошибок
- [ ] **AC-011.27**: При отсутствии Health Connect приложение работает корректно
- [ ] **AC-011.28**: При отказе в разрешениях показывается сообщение с инструкцией
- [ ] **AC-011.29**: При потере соединения показывается индикатор "Переподключение..."
- [ ] **AC-011.30**: При восстановлении соединения данные возобновляются

### UI/UX общие
- [ ] **AC-011.31**: Компонент данных с часов не мешает основному функционалу
- [ ] **AC-011.32**: Цветовые индикаторы зон пульса соответствуют спецификации
- [ ] **AC-011.33**: Данные обновляются не чаще 1 раза в секунду
- [ ] **AC-011.34**: При отсутствии данных показывается понятное сообщение

---

## Technical Requirements

### Architecture Impact

**Новые компоненты**:
| Компонент | Слой | Описание |
|-----------|------|----------|
| `SmartwatchData` | Domain | Модель данных с часов |
| `HeartRateZone` | Domain | Enum зон пульса |
| `HealthConnectRepository` | Domain | Интерфейс репозитория |
| `HealthConnectRepositoryImpl` | Data | Реализация репозитория |
| `ExerciseDetailViewModel` | Presentation | ViewModel для экрана упражнения |
| `SmartwatchDataCard` | UI | Composable компонент |

**Модифицируемые компоненты**:
| Компонент | Изменения |
|-----------|-----------|
| `ExerciseStats` | Добавить поля биометрии |
| `ExerciseStatsRepository` | Добавить метод сохранения с биометрией |
| `ExerciseDetailScreen` | Добавить SmartwatchDataCard + BiometricsComparison |
| `HomeScreen` | Добавить inline метрики в карточки упражнений и дня |
| `StatisticsScreen` | Добавить вкладку "Биометрия" с графиками |
| `AppModule` | Добавить DI для нового репозитория |
| `AndroidManifest.xml` | Добавить permissions |
| `build.gradle.kts` | Добавить зависимость Health Connect |

**Новые UI компоненты**:
| Компонент | Описание |
|-----------|----------|
| `SmartwatchDataCard` | Карточка реального времени для ExerciseDetailScreen |
| `ExerciseBiometricsInline` | Inline метрики для упражнений в HomeScreen |
| `DaySummaryBiometrics` | Суммарные метрики дня в HomeScreen |
| `BiometricsComparison` | Сравнение с предыдущей тренировкой |
| `HeartRateChart` | График пульса для StatisticsScreen |
| `CaloriesChart` | График калорий для StatisticsScreen |
| `HeartRateZoneDistribution` | Распределение зон пульса |
| `TopExercisesByCalories` | Топ упражнений по калориям |

### New Dependencies

```kotlin
// build.gradle.kts (app level)
dependencies {
    implementation("androidx.health.connect:connect-client:1.1.0-alpha07")
}
```

### API Changes

**Новые публичные API**:
- `HealthConnectRepository` - для доступа к данным Health Connect
- `SmartwatchData` - модель данных для UI

**Обратная совместимость**: Полностью сохранена. Функционал работает как дополнение.

### Test Coverage

- Unit тесты: > 85%
- Тестирование с mock Health Connect
- UI тесты:
  - SmartwatchDataCard (все состояния)
  - ExerciseBiometricsInline
  - BiometricsComparison
  - HeartRateChart
  - HeartRateZoneDistribution
- Integration тесты с реальным Health Connect (manual)

---

## Implementation Notes

### Порядок реализации

**Phase 1: MVP - ExerciseDetailScreen (2-3 дня)**
1. REQ-011.1: Базовая архитектура (модели, репозиторий, DI)
2. REQ-011.2: Расширение ExerciseStats (поля биометрии)
3. REQ-011.3: SmartwatchDataCard на ExerciseDetailScreen
4. REQ-011.4: Управление сессией (start/stop мониторинг)
5. REQ-011.5: Сохранение данных при завершении упражнения
6. REQ-011.7: Обработка ошибок и edge cases

**Phase 2: HomeScreen интеграция (1-2 дня)**
1. REQ-011.UI.2: Inline метрики в карточках упражнений
2. REQ-011.UI.2: Суммарные данные в карточке дня
3. REQ-011.UI.4: Сравнение с предыдущей тренировкой

**Phase 3: StatisticsScreen (1-2 дня)**
1. REQ-011.UI.3: Вкладка "Биометрия"
2. REQ-011.UI.3: Графики пульса и калорий
3. REQ-011.UI.3: Распределение по зонам
4. REQ-011.UI.3: Топ упражнений по калориям

### Рекомендуемый подход

1. **Создать абстракцию** - интерфейс HealthConnectRepository позволит в будущем добавить поддержку других платформ
2. **Graceful degradation** - приложение должно работать полностью даже без Health Connect
3. **Кэширование данных** - сохранять полученные данные локально на случай потери соединения

### Риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Health Connect недоступен на старых Android | Средняя | Среднее | Fallback к Google Fit API или скрытие функционала |
| Производители не поддерживают все типы данных | Высокая | Низкое | Показывать только доступные данные |
| Пользователь не предоставит разрешения | Средняя | Низкое | Четкое объяснение пользы, graceful degradation |
| Расход батареи при постоянном мониторинге | Средняя | Среднее | Оптимизация частоты обновления (1 сек) |
| Изменения API Health Connect | Низкая | Высокое | Использование стабильных версий, abstraction layer |

### Performance Considerations

- Обновление UI не чаще 1 раза в секунду
- Использование Flow для реактивного получения данных
- Кэширование последнего состояния для быстрого отображения
- Отписка от Flow при уходе с экрана

### Security Considerations

- Биометрические данные - чувствительная информация
- Хранить только с согласия пользователя
- Не передавать третьим лицам
- Соблюдать требования GDPR/локального законодательства

---

## Validation Plan

### Unit Tests

```kotlin
// HealthConnectRepositoryImplTest.kt
- testCheckAvailability_returnsTrue()
- testCheckAvailability_returnsFalse_whenNotInstalled()
- testRequestPermissions_granted()
- testRequestPermissions_denied()
- testGetCurrentData_returnsData()
- testGetCurrentData_returnsNull_whenNoSession()
- testGetRealtimeDataFlow_emitsData()

// SmartwatchDataTest.kt
- testHeartRateZoneCalculation()
- testHeartRateZone_rest()
- testHeartRateZone_fatBurn()
- testHeartRateZone_cardio()
- testHeartRateZone_hard()
- testHeartRateZone_maximum()
- testHeartRateZone_extreme()
```

### Manual Testing

1. **Тестирование на устройстве с Health Connect**:
   - Установить приложение
   - Открыть экран упражнения
   - Предоставить разрешения
   - Проверить отображение пульса
   - Проверить отображение калорий
   - Завершить упражнение
   - Проверить сохранение данных

2. **Тестирование без Health Connect**:
   - Установить на устройство без Health Connect
   - Проверить, что приложение работает
   - Проверить сообщение о недоступности

3. **Тестирование отказа в разрешениях**:
   - Отказать в разрешениях
   - Проверить сообщение
   - Перейти в настройки, включить
   - Проверить возобновление работы

### Edge Cases

- Устройство без Health Connect
- Пользователь отозвал разрешения во время сессии
- Часы отключились во время тренировки
- Некорректные данные от часов (пульс = 0 или 300)
- Быстрое переключение между упражнениями
- Приложение свернуто во время тренировки

---

## Dependencies

- Depends on: None
- Blocks: REQ-011.6 (Phase 2)
- Related to: 
  - REQ-001 (статистика упражнений)
  - REQ-006 (оптимизация статистики)

---

## Metrics

### Success Metrics

- Процент пользователей с включенной интеграцией: > 30%
- Retention пользователей с интеграцией vs без: +15%
- Среднее время сессии пользователей с интеграцией: +10%

### Technical Metrics

- Время загрузки данных с часов: < 500ms
- Потребление батареи: < 2% в час
- Crash rate при использовании интеграции: < 0.1%

---

## Open Questions

- [ ] **Q1**: Нужно ли поддерживать Google Fit API для Android < 14?
  - Рекомендация: MVP только Health Connect, Google Fit как Phase 3

- [ ] **Q2**: Нужно ли разрабатывать Wear OS приложение для часов?
  - Рекомендация: Нет, использовать Health Connect который уже работает с Wear OS часами

- [ ] **Q3**: Какие конкретные модели часов тестировать?
  - Рекомендация: Samsung Galaxy Watch, Garmin, Fitbit (через Health Connect)

- [ ] **Q4**: Нужна ли синхронизация данных с облаком?
  - Рекомендация: MVP - только локальное хранение, облако как отдельная фича

---

## References

### Documentation
- [Health Connect API](https://developer.android.com/health-and-fitness/guides/health-connect)
- [Health Connect Permissions](https://developer.android.com/health-and-fitness/guides/health-connect/plan/manage-permissions)
- [Android Health Data Types](https://developer.android.com/health-and-fitness/guides/health-connect/data-types)

### Code References
- `SYSTEM_PROMPT.md` - архитектура приложения
- `ARCHITECTURE.md` - паттерны проектирования
- `ExerciseDetailScreen.kt` - текущий экран упражнения
- `ExerciseStats.kt` - текущая модель статистики

---

## Appendix A: Расчет зон пульса

Формула максимального пульса (базовая):
```
MaxHR = 220 - age
```

Зоны пульса:
| Зона | % от MaxHR | Эффект |
|------|------------|--------|
| REST | < 50% | Восстановление |
| FAT_BURN | 50-60% | Жиросжигание |
| CARDIO | 60-70% | Кардио тренировка |
| HARD | 70-80% | Иненсивная работа |
| MAXIMUM | 80-90% | Максимальная интенсивность |
| EXTREME | > 90% | Экстремальная нагрузка |

Пример для 30 лет (MaxHR = 190):
- REST: < 95 уд/мин
- FAT_BURN: 95-114 уд/мин
- CARDIO: 114-133 уд/мин
- HARD: 133-152 уд/мин
- MAXIMUM: 152-171 уд/мин
- EXTREME: > 171 уд/мин

---

## Appendix B: Сравнение платформ

| Платформа | Health Connect | Samsung Health | Google Fit | Wear OS |
|-----------|----------------|----------------|------------|---------|
| Android версия | 14+ | Все | Все | Все |
| Samsung | ✅ | ✅ | ✅ | ✅ |
| Garmin | ✅ | ❌ | ✅ | ❌ |
| Fitbit | ✅ | ❌ | ✅ | ❌ |
| Xiaomi | ✅ | ❌ | ✅ | ❌ |
| Huawei | ⚠️ | ❌ | ❌ | ❌ |
| Сложность | Средняя | Низкая | Средняя | Высокая |
| Рекомендация | **Да** | Нет | Нет | Нет |

---

**End of Document**
